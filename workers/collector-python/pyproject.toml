[build-system]
requires = ["setuptools>=75"]
build-backend = "setuptools.build_meta"

[project]
name = "self-deepsearch-collector"
version = "0.1.0"
description = "Fixture-only collection contract simulator for Release A"
requires-python = ">=3.12"

[tool.setuptools.packages.find]
include = ["collector*"]
